#!/usr/bin/env bash# -----------------------------------------------------------------------------# Install script for git-upload-anything# -----------------------------------------------------------------------------set -euo pipefailTARGET="/usr/local/bin/git-upload-anything"echo "[INFO] Making script executable..."chmod +x git-upload-anything.shecho "[INFO] Copying script to $TARGET ..."sudo cp git-upload-anything.sh "$TARGET"echo "[INFO] Installation complete! You can now run:"echo "      git-upload-anything --help"